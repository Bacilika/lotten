using Godot;

namespace Lotten.Scripts.Products;

public class DrawableCircle(int radius, Vector2 position, Color color)
{
    public int Radius = radius;
    public Vector2 Position = position;
    public Color Color = color;
}